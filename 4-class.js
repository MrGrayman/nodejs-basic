class Calculate {
    static sum(x, y) {
        return x + y;
    }

    sum2(x, y) {
        return x + y;
    }

    static multiply(x, y) {
        return x * y;
    }
}
// const calculatorObject = new Calculate();
// console.log(Calculate.sum(1, 2));
// console.log(Calculate.sum2(2, 3));
// console.log(Calculate.sum(5, 6));
console.log(Calculate.multiply(5, 6));